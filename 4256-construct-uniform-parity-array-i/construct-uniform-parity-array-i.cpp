class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        return true;
    }
};
//Yes, literally always true for Problem I, because:

// all same parity → already uniform
// mixed parity → operations can make them uniform